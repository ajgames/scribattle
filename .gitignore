.DS_Store
.env
/node_modules/

# React Router
/.react-router/
/build/
/logs/

# Server build output
/server/dist/

# Drizzle: migration SQL + meta/ (journal & snapshots) are tracked so prod
# migrations are reproducible from a clean checkout.

# local-only architecture reference (its own git repo)
/realtime-example-battleroll/

# old prototype's compiled binary
/old-server/scribattle-server

# old prototypes (own git repos, kept local-only)
/old-client/
/old-server/
